// Concept: Any input → structured scene data
interface SceneSpec {
  id: string;
  location: {
    realWorld: { lat: number; lng: number; address: string };
    description: string;
  };
  characters: Character[];
  events: Event[];
  duration: number;
  keyFrames?: string[]; // paths to extracted video frames
}